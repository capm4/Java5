package Module3.HW.HomeWork_3_2;

public class Adder extends Arithmetic {
     public boolean check(Integer a, Integer b){
        boolean checkInt = false;
        if (a >= b) checkInt = true;
        return checkInt;
    }
 }
